rootProject.name="wimageviewer"
pluginManagement{
    repositories {
        maven {
            setUrl("https://nexus.gluonhq.com/nexus/content/repositories/releases")
        }
        gradlePluginPortal()
    }
}
